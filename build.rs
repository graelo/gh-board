fn main() {
    // Tree-sitter grammar crates handle their own compilation via build scripts.
    // This build.rs is reserved for any future build-time logic (e.g., embedding
    // git commit hash for version display).
}
